language: node_js
dist: xenial
node_js:
  - node
cache:
  directories:
    - node_modules
branches:
  only:
    - master
install:
  - npm install
script: npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: master
